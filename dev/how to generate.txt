cd to the directory where pelicanconf.py reside
then assuming sudo pip install pelican and sudo pip install markdown were run on the machine execute in command line:
pelican content

to generate output (and monitor for changes): make regenerate
to launch the server run: make serve
then browse to localhost:8000